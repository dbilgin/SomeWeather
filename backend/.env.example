# PostgreSQL connection string from Dokploy
DATABASE_URL=postgresql://user:password@internal-host:5432/dbname

# API key for authorization (X-API-Key header)
WEATHER_API_KEY=your-api-key-here

# Server port (default: 3000)
PORT=3000
